# Configuration de l'environnement pour Epic Events CRM
# ======================================================
# Ce fichier est un template. Copiez-le en .env et remplissez vos valeurs.
# Le fichier .env ne doit JAMAIS être commité dans Git (il est dans .gitignore).

# Base de données PostgreSQL
# --------------------------
DB_HOST=localhost
DB_NAME=epicevents_db
DB_USER=votre_nom_utilisateur
DB_PASSWORD=votre_mot_de_passe_si_necessaire
DB_PORT=5432

# Clé secrète pour JWT
# --------------------
# IMPORTANT: Générez une clé secrète forte et unique pour la production
# Vous pouvez utiliser: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=votre_cle_secrete_tres_longue_et_aleatoire_a_generer

# Sentry (Monitoring optionnel)
# ------------------------------
# Décommentez et remplissez si vous utilisez Sentry pour le monitoring
# SENTRY_DSN=https://votre_cle@sentry.io/votre_projet

# Configuration de l'application
# -------------------------------
# Durée de validité des tokens JWT en heures (par défaut: 24h)
TOKEN_EXPIRY_HOURS=24

# Mode de l'application (development ou production)
APP_MODE=development

# Notes de sécurité
# -----------------
# 1. NE JAMAIS commiter le fichier .env dans Git
# 2. Utiliser des mots de passe forts pour la base de données
# 3. Générer une SECRET_KEY unique et aléatoire pour chaque environnement
# 4. En production, changer toutes les valeurs par défaut
# 5. Limiter les accès à la base de données aux seules IP autorisées
